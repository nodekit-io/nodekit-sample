{
  "name": "__PROJECT_NAME__",
  "version": "1.0.0",
  "description": "Sample {NK} NodeKit application created by NodeKit Command Line tools",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nodekit-io/nodekit-sample.git"
  },
  "keywords": [
    "nodekit"
  ],
  "author": "__AUTHOR__",
  "license": "Apache-2.0",
  "devDependencies": {
    "react-scripts": "0.8.4"
  },
  "nodekit": {
     "main": "app://app.nkar/index.html",
     "unused-singlepageapp": "app://app.nkar/index.html",
      "unused-electroapp": "app.nkar/electro.js"
  },
  "dependencies": {
    "mobx": "^2.7.0",
    "mobx-react": "^4.0.3",
    "moment": "^2.17.1",
    "react": "^15.4.1",
    "react-dom": "^15.4.1",
    "offgrid-components": "^0.1.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build && rm -rf dist && cd build && cp -f ../package.json ./ && cp -f ../electro.js ./ && rm -rf ../dist && mkdir ../dist && zip -r ../dist/app.nkar . && cd ..",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
